<!doctype html>

<html>
<head>
	<title>HCI Project Portfolio</title>	
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<link href="/css/bootstrap.min.css" rel="stylesheet">
	<link href="/css/bootstrap-theme.min.css" rel="stylesheet">
	<link href="/css/introHCI.css" rel="stylesheet">
	<!-- this is a comment in HTML -->

    <!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
      <script src="https://oss.maxcdn.com/libs/respond.js/1.3.0/respond.min.js"></script>
    <![endif]-->        
</head>

<body>
	<div class="container">
		<div class="table">
		    <div class="tr">
		        <div class="d1">
		        	<span class="btn-group">
						<a href="/view" class="btn">
							<pre>&lt; </pre>
						</a>
					</span>
		        </div>
		        <div class="d2">	
					<span class="header">
						<span class="btn-group">Invitations</span>
					</span>
		        </div>
		        <div class="d3"></div>
		    </div>
		</div>

		<hr>

		<div class="table">
			<form>
				{{#each invites}}
				    <div class="tr">
				        <div class="d12">	
							<span class="list">
								<span class="btn-group">{{organizer}} invited you to {{eventName}} at {{eventLocation}} for {{eventDuration}} hours!</span>
							</span>
				        </div>
				        <div class="d3">
				        	<input type="submit" id="{{id}}" class="btn btn-primary" value="Confirm!"></input>
				        </div>
				    </div>
				    <hr>
				{{/each}}   

		   	</form>
		</div>


		<!-- {{#each invites}}
	      <div class="invite" id="{{id}}">
			<a href="project" class="thumbnail">
	          <p>{{organizer}} invited you to {{eventName}} at {{eventLocation}} for {{duration}} hours!</p>
	        </a>

	      	<div class="center-block">
		   		<input type="submit" id="submitBtn" class="btn btn-primary" value="Confirm!"></input>
	   		</div>
	        
	      </div>
	    {{/each}} -->

	






	</div>
	<script src="https://code.jquery.com/jquery.js"></script>
	<script src="/js/bootstrap.min.js"></script>
</body>
</html>
